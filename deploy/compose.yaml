services:
  web:
    image: ${IMAGE_NAME:-personweb-web}
    container_name: ${CONTAINER_NAME:-personweb}
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${BIND_ADDRESS:-0.0.0.0}:${APP_PORT:-8080}:80"
